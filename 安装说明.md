# 行为门 · 安装说明（拷到别的电脑用这个）

> 版本 V3.5.3 ｜ 2026-09-14
>
> ⚠️ 这门能拦什么、**不能**证明什么，见包内 `install.md` 的「已知边界」。
>
> 版本声明以 `adapters/claude-code/hooks/VERSION` 为准（它随 hooks 一起部署与校验）；
> 本文档与它不一致时，以 VERSION 为准。

---

## 这是什么

给 Claude Code 加**五道机械门**，治的是 AI 助手"想多做、做了却说完成了"这类毛病：

| 门 | 拦什么 |
|---|---|
| **G1 预算门** | 默认不许派子代理；要派就在消息里写 `agent_spawns: N` |
| **G3 生效门** | 说"已完成/已修复"时必须给证据，否则打回 |
| **G5 危险门** | `pkill -f`、`find /tmp`、整行 `rm -rf /` 这类会翻车的写法 |
| **G7 意图门** | 你说过"别再问我"就不再弹框；说过"不用测试"就不再跑测试 |
| G6 收口 | 记录（默认关闭） |

**中文英文都认。所有提示都是中文。**

---

## 拷到新电脑怎么做

### 第一步：准备

需要 **Python 3.8 或更高**。

检查有没有：

```bash
python --version
```

没有的话去装：https://www.python.org/downloads/windows/
**安装时务必勾选 "Add Python to PATH"。**

> 没装 Python 也能装这套门 —— 但门不会工作（安装脚本会明确告诉你）。

### 第二步：拷贝

把整个文件夹拷到新电脑任意位置。

### 第三步：预演（不写任何文件）

```bash
cd <你拷到的位置>
python install.py
```

它会告诉你要改什么、保留什么。

### 第四步：安装

**装到全局（所有项目都生效）—— 推荐：**

```bash
python install.py --scope global --apply
```

**或者只装到某个项目：**

```bash
python install.py --project "D:/我的项目" --apply
```

### 第五步：看自检结果

安装脚本最后会**自己跑一遍五道门**，输出应该是：

```
[6/6] 自检：用真实注册的命令跑一遍五道门 …
      [OK] G1 预算门 (rc=2)
      [OK] G5 危险门 (rc=2)
      [OK] G3 生效门 (rc=2)
      [OK] 放行路径 (rc=0)

安装完成（全局（影响所有项目）），自检通过。
```

**看到"自检通过"才算装好。** 如果不是，把输出发给开发者 —— 不要当成装好了。

### 第六步：重启 Claude Code

重启后生效。

---

## 装完怎么验

在 Claude Code 里发这几句：

```
帮我调研一下这个项目
```
→ 应该被 G1 拦下，并告诉你两条出路。

```
帮我调研一下这个项目，agent_spawns: 2
```
→ 这次会放行 2 个子代理。

```
我已经修复完成了
```
→ 应该被打回，要求补证据。

---

## 常用操作

**放行子代理**（不用改文件，直接在消息里写）：
```
帮我调研这个项目，agent_spawns: 3
```

**临时豁免测试**（当你说过"不用测试"但又想跑）：
```
verify: on
```

**临时允许提问**（当你说过"别再问我了"但又想让我问）：
```
questions: on
```

---

## 卸载 / 回滚

安装脚本每次都会备份，并打印回滚命令。手动的：

```bash
# 全局
copy "C:\Users\<你>\.claude\settings.json.bak-<时间戳>" "C:\Users\<你>\.claude\settings.json"
```

（把 `<你>` 换成你的用户名。）

---

## 如果出问题

**门没反应？** 先跑自检：

```bash
python tools/verify_portable.py
```

它会模拟在一台新电脑上安装并运行，13 项全过说明包本身没问题。

**提示看不懂？** 所有门的提示都是中文，且会说明"为什么拦"和"该怎么办"。

---

## 技术说明（可跳过）

- **零第三方依赖** —— 只用 Python 标准库
- **Python 路径是安装时探测的**，不是写死的（换电脑不会失效）
- **不覆盖你的已有配置** —— 只新增条目，不删除任何东西
- **不自动杀进程** —— 只记录
- **拦不住的事**：换 Python/Node 写脚本做同样的危险动作；证据的真假

定位：**安全带，不是自动驾驶。**
