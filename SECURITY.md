# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| 3.5.x | ✅ 支持 |
| < 3.5 | ❌ 不再支持 |

## 报告漏洞

**请不要在公开 Issue 中披露漏洞细节、凭据、Cookie、存储状态、私有 URL 或个人数据。**

- 优先使用 [GitHub 私下漏洞报告](https://github.com/haotianshuo/behavior-gate/security/advisories/new)
- 或发邮件至 `xrlcom@126.com`，主题注明 `Security report — behavior-gate`

`xrlcom@126.com` 是**公开的项目联系地址，不是版权人声明**。

报告中请包含：受影响的版本、操作系统/运行时、最小化的脱敏复现步骤、影响范围、相关文件或门。

我们在 **3 个工作日内**确认收到可用报告；修复时间取决于严重程度与可复现性。

## 披露政策

- 我们会在修复发布后公开致谢（除非你要求匿名）
- 在修复发布前，请勿公开细节

---

## Threat Model —— 本项目防什么、不防什么

**这是理解本项目安全边界的关键。请先读这一节再报告漏洞。**

本项目是一个**行为约束层**，不是安全产品。它明确声明：

| 威胁 | 状态 | 说明 |
|------|------|------|
| T1 误操作 / 本地损坏 / 漂移 | ✅ **当前重点保护** | 这是设计目标 |
| T2 有工作目录写权限的恶意操作者 | ❌ **NOT_PROTECTED** | 不构成有效防护 |
| T3 多方审计 / 第三方不可抵赖 | ❌ **OUT_OF_SCOPE** | 不在设计范围内 |

### 已知可绕过路径（**不是漏洞，是已知边界**）

以下情况**不需要报告**，因为它们是已知且已声明的事实：

```text
G5 危险门只检查 Bash 命令字符串。
  用 Python 写 os.remove() / shutil.rmtree() 可以完全绕过。
  → 它是防误操作层，不是安全边界。

G3 生效门只检查证据的【格式】，不检查【真伪】。
  写 "证据：L3 端到端" 但不真的验证，可以通过。
  → 它防的是「忘记给证据」，不是「伪造证据」。

本项目的失败策略是 FAIL-OPEN（宁可漏拦，不可卡死会话）。
  → 门的 JSON 解析失败时会放行，这是设计选择。
```

### 欢迎报告的漏洞

```text
- 危险门规则可被【非预期】构造绕过（不是上面已声明的那些）
- 门的异常导致会话卡死（违反 FAIL-OPEN 承诺）
- 路径遍历 / 命令注入（如 install.py 处理恶意输入）
- 安装过程写入非预期位置
- 静默失败（门没生效但不报错）—— 这是本项目最重视的缺陷类型
```

**静默失败是本项目定义的「最严重缺陷」**：
看起来装了门，其实门是假的。如果你发现这种情况，请务必报告。

## 不在范围内

```text
- 需要物理访问的攻击
- 上游依赖的漏洞（本项目零第三方依赖）
- 用户自己修改 policy/*.json 导致的行为变化
- Claude Code 本身的漏洞（请向 Anthropic 报告）
```
