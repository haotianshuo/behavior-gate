# 变更记录

> 只记**真实发生过的问题**与对应的修复。每条都注明：出现在哪版、修于哪版、谁发现的。
> 编号在整个项目内唯一，方便回指（如「见 #12」）。

## 版本线

| 版本 | 做了什么 |
|---|---|
| V2.6 | 基线。五道门 + 安装脚本 + 三套测试 |
| V2.7 | 删掉 5 个坏 `.cmd` 包装器；新增废弃文件清理、命令归属判定 |
| V2.8 | 修 `_is_ours` 双向判错；清理提到漂移检查之前 |
| V2.9 | 拆开预检/终检判据（`--require-complete`）；扩展名对齐；`.bak` 清理 |
| V3.0 | 备份归属改按基名；复制改递归（支持子目录） |
| 3.1.0 | 新增 `VERSION` 版本身份；修 G3 引用误拦与空行绕过；漂移感知版本变更 |
| 3.2.0 | 锁加持有者身份（fencing）；规则收敛到 `lib/deploy_files.py` |
| 3.3.0 | 部署校验的输出带 Claim 边界 |
| 3.4.0 | 收窄过度声明；合并重复文档 |
| 3.5.0 | G5 提示文案说清"纯字符串匹配"这一事实｜收窄过度声明 |
| 3.5.1 | 修 POSIX 上 `NotADirectoryError` 被误判为未知异常 → 用户永久卡死（CI 首次公开运行时发现） |
| 3.5.2 | 修 Windows 控制台 GBK 下中文输出崩溃 / 乱码（CI 暴露；影响 install.py、全部测试与门告警） |
| 3.5.3 | 全局 policy 副本静默覆盖包内修改（#29）；测试未隔离环境（#30）；closeout 开关无人读（#31） |
| 3.5.4 | 文档未同步 3.5.3 的行为变更：边界表仍写「Bash/WebFetch 没有」 |
| 3.5.5 | 重复命令检测无人读（#33）；陈旧警告每次调用都刷屏（#34）；边界表措辞像欠债（#35） |
| 3.5.6 | 死段 gateway_budget 整段没人读（#36）；verify_deploy 新增死段自动检测 |
| 3.5.7 | 死段检测器结构性漏判（#38）；两个隐藏死键（#39）；README 项数再次过期（#40）|
| 3.5.8 | 自定义规则格式错误被静默忽略（#41）；`cmd_deny`/`content_deny` 未在 policy 中声明（#42）|
| 3.5.9 | G3 五处缺陷（#43–#45）：模板字段成逃逸钥匙、重试即通过、Windows 路径不拦、词表漏网 |
| 3.5.10 | G1 无 use-mention 剥离：引用别人的文字里的 `agent_spawns: N` 被当成真实指令（#47）|
| 3.5.10 | G5 `rm_rf` 族三项缺陷：前缀贪婪误报 + 短选项分离/大写 R 漏报 + MCP 通道靠缺陷侥幸生效（#48）|
| 3.5.10 | 部署判定把「未发布版本的合法修订」误判为篡改 —— 拆分 DEPLOYED_INTEGRITY 与 SOURCE_IDENTITY 两层（#49）|
| 3.5.10 | Gate 行为无法被低成本观测 —— 为分析一次真实使用要反向扫 3.5 万条消息；新增最小 Gate 事件记录（#50）|
| 3.5.10 | G5 `rm_rf_windows_drive` 第三分支无锚定 → 用户目录下的深层路径全被误拦（#51）|
| 3.5.10 | `install.py` 静默忽略未知参数 → README 的 `--global` 从未生效，静默装错位置（#52）|

> 从 3.1.0 起版本号由 `adapters/claude-code/hooks/VERSION` 声明，随 hooks 一起部署与校验。
> **改了 hook 内容就必须同时改 VERSION** —— 否则升级会被自己的漂移检查拦下（这是设计意图）。

## 全部问题与修复（52 项）

严重度：🔴 高 / 🟡 中 / ⚪ 低

| # | 问题 | 级别 | 出现于 | 修于 | 谁 |
|---|---|---|---|---|---|
| 1 | 备用启动器写死 Python 路径 → 异机静默放行 | 🔴 | V2.6 | V2.7 | 作者 |
| 2 | 清理排在漂移检查之后 → 升级路径执行不到 | 🟡 | V2.7 | V2.8 | 作者 |
| 3 | `_is_ours` 用 `bg-` 匹配整条命令 → 误删路径含 `bg-` 的第三方 hook | 🔴 | V2.7 | V2.8 | 作者 |
| 4 | 白名单漏 `intent_gate` → 换机后死条目累积 | 🟡 | V2.7 | V2.8 | 作者 |
| 5 | 缺文件被判 `MATCH` → 边列 `MISSING` 边判通过 | 🔴 | V2.8 | V2.9 | 作者 |
| 6 | 清单要 `.json`、复制只做 `.py/.cmd` → 永远装不上 | 🟡 | V2.8 | V2.9 | 作者 |
| 7 | `.bak` 保留数文案与实际不符（3 vs 5） | ⚪ | V2.9 | V3.0 | 作者 |
| 8 | `.bak` 清理删掉用户自己的备份 | 🔴 | V2.9 | V3.0 | 作者 |
| 9 | `listdir` vs `rglob` 不对称 → 子目录文件永远装不上 | 🟡 | V2.9 | V3.0 | 作者 |
| 10 | 按文件名判归属 → 第三方 `_lib.py` 被删 | 🟡 | V2.7 | 3.2.0 | 复核 |
| 11 | 删完文件后空目录残留 | ⚪ | V2.7 | 3.2.0 | 复核 |
| 12 | G3 主路径未剥离引用 → 引用触发词即误拦 | 🔴 | V2.6 | 3.1.0 | 复核 |
| 13 | G3 一个空行即可绕过"未验证"判定 | 🟡 | V2.6 | 3.1.0 | 复核 |
| 14 | 包无机器可读版本身份（文档写 V2.4、实际 V3.0） | 🟡 | V2.6 | 3.1.0 | 复核 |
| 15 | 锁无持有者身份 → 被夺锁后仍写 → 丢更新 | 🟡 | V2.6 | 3.2.0 | 复核 |
| 16 | 清单无 `status` 字段 → 失败与成功写成一样 | ⚪ | V2.7 | 3.1.0 | 复核 |
| 17 | 锁等待 8s 长于 hook 预算 5s → 该分支是死代码 | ⚪ | V2.6 | 3.2.0 | 复核 |
| 18 | `STALE` 拦住一切改了 hook 的升级，必须 `--force` | 🟡 | V2.8 | 3.1.0 | 复核 |
| 19 | 同一条规则写在两处（补 #14 时新造的第三处） | 🟡 | 3.1.0 | 3.2.0 | 复核 |
| 20 | #10 残余：老清单回退里含第三方文件 | ⚪ | V2.7 | 3.2.0 | 复核 |
| 21 | 部署校验自称"部署**生效**校验"，证明不了"生效" | 🟡 | V2.6 | 3.3–3.4 | 复核 |
| 22 | `install.py` 里同样写着"部署生效校验" | ⚪ | V2.6 | 3.4.0 | 复核 |
| 23 | 锁自称"跨进程互斥"但未限定范围（本地/分布式） | ⚪ | V2.6 | 3.4.0 | 复核 |
| 24 | 独立边界文档与 `install.md` 已有内容重复 | ⚪ | 3.3.0 | 3.4.0 | 复核 |
| 25 | 版本变更时输出 `MATCH` + `STALE` 的自相矛盾形状 | ⚪ | 3.1.0 | 3.3.0 | 复核 |
| 26 | G5 拦"写文件时提到危险词"（`Write` 放行、Bash heredoc 拦） | 🟡 | V2.6 | 3.5.0 | 复核 |
| 27 | POSIX 上 `NotADirectoryError` 掉进"未知异常"分支 → 环境故障被当成保守拒绝 → 用户永久卡死 | 🔴 | 3.1.0 | 3.5.1 | CI |
| 28 | Windows 控制台 GBK 下中文输出：轻则乱码、重则 `UnicodeEncodeError` 崩溃（`_Lock` 告警裸写 stderr） | 🔴 | V2.6 | 3.5.2 | CI |
| 29 | `~/.claude/behavior-policy.json` 旧副本优先于包内策略 → 改了 policy 不生效，且无提示 | 🔴 | V2.6 | 3.5.3 | 开发实测 |
| 30 | 测试未隔离 policy 环境 → 同一份代码在不同机器上结果不同 | 🟡 | V2.6 | 3.5.3 | 修 #29 时暴露 |
| 31 | `closeout.enabled` 声明与行为不符：policy 写 false，门却不读它、无条件运行 | 🟡 | V2.6 | 3.5.3 | 复核 |
| 32 | 改了行为但没改声明：3.5.3 让用量提示生效，边界表却仍写「Bash/WebFetch 没有」 | 🟡 | 3.5.3 | 3.5.4 | 用户发现 |
| 33 | `repeat_suspects`（重复命令检测）只写不读 —— 和 bash 计数同一个毛病 | 🟡 | V2.6 | 3.5.5 | 用户促成 |
| 34 | 3.5.3 新增的配置陈旧警告【每次调用都打印】→ 一个会话几十次调用刷屏 = 等于没提示 | 🟡 | 3.5.3 | 3.5.5 | 实测发现 |
| 35 | 边界表把设计边界写成「❌ 未解决」，读起来像欠债清单 | ⚪ | V2.6 | 3.5.5 | 用户指出 |
| 36 | `gateway_budget` 整段没有任何 hook 读取 —— 第三次出现同形状缺陷 | 🟡 | V2.6 | 3.5.6 | 外部复核 |
| 37 | `verify_deploy` 只查两个写死的死键，没有通用死段检测 | 🟡 | V2.6 | 3.5.6 | 修 #36 时暴露 |
| 38 | 死段检测器判据太宽：段名作为【任意字面量】即算被读 —— `DEFAULT_POLICY` 给每段都写了名字，导致结构性漏判 | 🔴 | 3.5.6 | 3.5.7 | 外部复核 |
| 39 | `effect_gate.min_risk_level` / `destructive_gate.deny_patterns` 只在 `DEFAULT_POLICY` 里，无 `.get()` 访问 —— 用户改 policy 时甚至看不见它们 | 🟡 | V2.6 | 3.5.7 | 新检测器抓到 |
| 40 | README 的「N 项真实问题」再次过期（#32 修过一次，同类复发）| ⚪ | 3.5.6 | 3.5.7 | 本体发现 |
| 41 | `_compile` 用 `except: continue` 静默吞掉格式错误的规则 —— 用户加了自定义规则却没生效，且无任何提示 | 🔴 | V2.6 | 3.5.8 | 外部复核 |
| 42 | `cmd_deny` / `content_deny` 被代码读取，但不在 policy 里 —— 用户看不见这个能生效的开关 | 🟡 | V2.6 | 3.5.8 | 外部复核 |
| 43 | G3 要求写「未验证项：无」，而该字段被当成"承认有未验证"→ 风险高+L1 被放行（门自己要求的字段成了逃逸钥匙）| 🔴 | V2.6 | 3.5.9 | 外部复核 |
| 44 | `stop_hook_active=true` 无条件放行 → 打回后原样重发即通过，强制力从"最多 8 次"降到 1 次 | 🟡 | V2.6 | 3.5.9 | 外部复核 |
| 45 | G5 只认 POSIX 根路径 → Windows 上 `rm -rf D:/`、`C:/Users/` 全部放行（本机是 Win11）| 🔴 | V2.6 | 3.5.9 | 外部复核 |
| 46 | G3 完成词表漏网 6/10（「全部搞定，收工。」「任务结束。」等常见收尾全放行）| 🟡 | V2.6 | 3.5.9 | 外部复核 |
| 47 | G1 解析预算时对整条 prompt 做匹配，无 use-mention 剥离 —— 用户引用/讨论里出现的 `agent_spawns: N` 被当成真实声明 | 🔴 | V2.6 | 3.5.10 | 用户实测（贴长篇点评时触发）|
| 48 | G5 `rm_rf` 族三项缺陷：正则无结尾锚定（`rm -rf /tmp/x` 被前缀贪婪误拦）＋ 不认短选项分离与大写 R（`rm -r -f /`、`rm -Rf /` 漏拦）＋ MCP 通道的 rm 保护一直靠该缺陷侥幸生效 | 🔴 | V2.6 | 3.5.10 | 真实会话（09-15 09:51 误拦临时目录清理）|
| 49 | 部署判定用「版本号是否相同」当两个概念的代理 → 未发布版本（3.5.10 无 tag/Release）的合法修订被判成 `SOURCE_IDENTITY_DRIFT` 并拦下；同时「篡改部署 + 顺手改 VERSION」这条路径旧判据会放行 | 🔴 | 3.1.0 | 3.5.10 | 本轮实测（对照实验坐实）|
| 50 | Gate 不记录自己的决定（只记工具调用）→ G1/G3/G5/G7 的真实收益与误伤长期 NOT_MEASURED。为分析 16 次真实干预需反向扫描约 3.5 万条消息 | 🔴 | V2.6 | 3.5.10 | 本轮实测（逐条读真实对话时暴露）|
| 51 | `rm_rf_windows_drive` 第三分支无结尾锚定 → `C:/Users/<用户>/任意/更深/路径` 全被当成「删用户目录」。实测 4 个用例误报（`node_modules`/`.cache`/`AppData/Local/Temp`/`Downloads`），而该规则的注释【自己写着】「更深的项目路径不拦」 | 🟡 | 3.5.9 | 3.5.10 | 另一台电脑复核（本机复现）|
| 52 | `install.py` 静默忽略未知参数 → README 写的 `--apply --global` 从未被解析，静默降级成项目级，且因 `--project` 默认取当前目录而装进【包自己所在的目录】。用户以为装了全局，重启后门完全不生效 | 🔴 | V2.6 | 3.5.10 | 两台电脑独立复核（本机实测坐实）|

**统计**：共 52 项。🔴 17 项、🟡 24 项、⚪ 11 项。
发现者分布：见每行的「谁」列 —— 作者 / 复核 / CI / 外部复核 / 用户 / 本体。

> **#24 和 #25 是复核自己引入的** —— 和作者"修 A 引入 B"是同一个毛病。
> 它们都被下一轮复核抓出，量级也在下降。

## 验证方式

每条修复都做了 **Before / After**，不是只看代码：

| # | 修前（实测） | 修后（同一复现重跑） |
|---|---|---|
| 3 | 路径含 `bg-` 的第三方 hook 被删 | 保住 |
| 5 | 7 个文件只装 2 个 → 判 `MATCH` | 判 `SOURCE_IDENTITY_DRIFT` |
| 8 | 用户自己的旧备份被删 | 保住，只清自家的 |
| 9 | 子目录文件永远装不上 | 装上、`MATCH` |
| 10 | 第三方 `_lib.py` 被删 | 保住 |
| 12 | 五种引用写法全被拦 | 全放行；裸词仍拦 |
| 13 | `已完成。` + 空行 + `未验证。` 放行 | 拦下 |
| 15 | 两个进程都报写入成功，计数少 1 | 一个写入、一个报 `LOCK_PREEMPTED` |
| 18 | V2.6 → 新版被中止 | 通过，不需 `--force` |
| 26 | 同内容：`Write` 放行 / Bash 拦 | 行为不变，#26 只改提示文案 |

## 没修的 / 修不了的

| 项 | 状态 |
|---|---|
| 幻觉、证据真假 | **原理上做不到**。门只能查"有没有写证据块"，查不了证据真假 —— 空洞的证据块照样能过。 |
| G5 的 heredoc 误拦（#26 的行为部分） | **只改了文案，行为未动**。在命令里做 use-mention 剥离不安全（`sh -c` 的引号里照样执行），放宽一道安全门需要单独评估。 |
| MCP `content` 字段走 content_rules（#48 已声明） | **有意保留**。字段分离后，MCP 的 `content` 只过 `content_deny` —— Edit/Write 一直如此。见下方「写入内容里的 Windows 盘符删根」。**但字段【分类判据】用裸子串是缺陷（见 #53 待修）**：`"text" in "context" == True`，导致 `context` 等与内容无关的字段被误归 content 类。 |
| `SOURCE_IDENTITY` 只绑 1 类事实 | 参考要求为 4 类（repo/harness/scoring/prompt authority）。**没有真实使用场景作为证据 → 不改**。 |
| 真实第二台电脑 | 未验证。换机一直是模拟的。 |
| 写入内容里的 Windows 盘符删根（#48 引入的已知边界） | **有意保留**。字段分离后，`content` 字段只过 `content_deny`，而 `rm_rf_in_content` 只覆盖 `(/ \| ~ \| $HOME)`，不含盘符 —— 所以 MCP 写文件内容里的 `rm -rf D:/` 不再被拦。**Edit / Write 工具一直就是这个行为**，不是新造的不一致。取舍：写入内容不直接执行；拦「真会执行的命令」是 Bash / MCP.command 通道的职责，那里盘符覆盖完整。已用测试锁住该行为（改它会红）。 |
| `rm_rf_in_content` 的多行覆盖（#48 顺带实测） | **未修**。`^[ \t]*` 无 `re.M`，只匹配字符串开头 —— 所以「脚本第 2 行是删根命令」拦不到（实测：`#!/bin/bash\nrm -rf /\n` 放行）。这是**既有行为**，本轮未触碰该规则的多行语义；要修需单独评估（加 `re.M` 会扩大拦截范围）。 |

## 已知边界

### Gate 事件记录（gate-events.jsonl）

```text
它证明：本机 Gate 代码记录了自己做出的决定
它不证明：日志不可修改 / 不可伪造 / 攻击者不能删除 / 事件不可抵赖
也不表示：事件一定代表真实世界风险（门只做字符串匹配）

T2 malicious-writer-proof → OUT_OF_SCOPE
T3 non-repudiation        → OUT_OF_SCOPE
```

刻意不加：hash chain / 签名 / 数据库 / attestation / 远端 telemetry。
本地观测不需要这些；加了就是把「看见行为」伪装成「可信审计」。

隐私契约（有测试锁定）：只记 `ts / session / gate_id / rule_id / decision /
tool / source_class / version / source_snapshot`。
**不记**完整 prompt、完整 assistant 输出、完整命令行、Write/Edit 正文、
MCP payload、环境变量值、任何疑似密钥。

`source_class` 是**测试 harness 能力**，不是用户产品功能 ——
它只用于把「真实使用」与「专门测试」分开，不让两者混进同一分母。
普通用户不需要知道它。



见 `install.md` 的「已知边界」段 —— 包括 Threat Model（T1 保护 / T2 不保护 / T3 超范围）。
**一句话：门管的是「行动方式」和「格式」，不是「判断对不对」。**
